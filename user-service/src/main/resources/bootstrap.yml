spring:
  application:
    name: userservice
  profiles:
    active: dev   #关联Nacos配置中心文件名-后的环境名称
  cloud:
    nacos:
      server-addr: http://localhost:80    #Nacos地址
      config:
        file-extension: yml